FROM python:3.11-slim

WORKDIR /app

COPY requirements-dashboard.txt .
RUN pip install --no-cache-dir -r requirements-dashboard.txt

COPY app.py .

EXPOSE 8889

CMD ["streamlit", "run", "app.py", "--server.port=8889", "--server.address=0.0.0.0"]
